// Print "You can drive" or "You can't drive" based on age being greater than 18 using ternary operator

let age = prompt("Enter your age:")

let license = (age > 18 ) ? "You can drive" : "You can't drive";

console.log(license);